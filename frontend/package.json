{
    "author": "wysiwys",
    "name": "amiga-webplayer",
    "version": "0.1.0",
    "type": "module",
    "scripts": {
        "build": "rimraf dist pkg && wasm-pack build --release --target web && tsc --noEmit && HAS_BACKEND=1 npx vite build",
        "buildOffline": "rimraf dist pkg && wasm-pack build --release --target web && tsc --noEmit && HAS_BACKEND=0 npx vite build"
    },
    "browser": {
        "fs": false
    },
    "devDependencies": {
        "@types/audioworklet": "^0.0.59",
        "prettier": "3.3.3",
        "rimraf": "^6.0.1",
        "rollup-plugin-license": "^3.5.2",
        "vite": "^5.4.0"
    },
    "dependencies": {
        "@vitejs/plugin-vue": "^5.1.2",
        "@vueuse/core": "^11.0.0",
        "idb": "^8.0.0",
        "pinia": "^2.2.1",
        "vue": "^3.4.37"
    }
}
